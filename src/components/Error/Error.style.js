import styled from "styled-components";
import { colors } from "../../utils";

export const ErrorWrapper = styled.div`
  color: ${colors.red};
  font-size: 11px;
  text-align: center;
  font-weight: 700;
`;
