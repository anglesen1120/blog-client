import styled from "styled-components";
import { colors } from "../../utils";

export const ItemContact = styled.section`
  color: ${colors.blue};
  text-decoration: underline;
  text-align: left;
  margin: 15px 0;
`;
